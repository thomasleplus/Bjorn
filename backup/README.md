# backup

Working directories for Bjorn's backup/restore feature. They ship empty
(`.gitkeep`) and are populated at runtime.

- `backups/` — generated backup archives of Bjorn's data/config.
- `uploads/` — backups staged for restoring.
