# config

Bjorn's configuration.

- `shared_config.json` — the main settings loaded at startup (via
  [`../init_shared.py`](../init_shared.py) / [`../shared.py`](../shared.py)) and
  editable from the web UI's config page. Controls which actions are enabled,
  timing, targets scope and display options.
